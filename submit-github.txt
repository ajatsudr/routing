git config --global user.name "xxx"
git config --global user.email "xxx"

…or create a new repository on the command line
echo "# test" >> README.md
git init
git add README.md
git commit -m "first commit"
git remote add origin https://github.com/ajatsudr/test.git
git push origin master

…or push an existing repository from the command line
git remote add origin https://github.com/ajatsudr/test.git
git push origin master

..or change remote

git remote set-url origin https://github.com/ajatsudr/test.git

.. or clone

git clone https://github.com/ajatsudr/test.git

untuk tarik pull, agar source local up to date
git pull origin master